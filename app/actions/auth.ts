// "use server"

// import { cookies } from "next/headers"

// export async function login(email: string, password: string) {
//     // Here you would typically:
//     // 1. Validate the input
//     // 2. Check the user's credentials against your database
//     // 3. Create a session or token

//     // For this example, we'll just set a cookie to simulate authentication
//     cookies().set("user", email, { secure: true, httpOnly: true })
// }

// export async function register(email: string, password: string) {
//     // Here you would typically:
//     // 1. Validate the input
//     // 2. Check if the user already exists
//     // 3. Hash the password
//     // 4. Store the user in your database
//     // 5. Create a session or token

//     // For this example, we'll just set a cookie to simulate authentication
//     cookies().set("user", email, { secure: true, httpOnly: true })
// }

